apiVersion: v1
kind: Service
metadata:
  name: {{ include "stub-civil-claims-api.fullname" . }}
  labels:
    {{- include "stub-civil-claims-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stub-civil-claims-api.selectorLabels" . | nindent 4 }}
