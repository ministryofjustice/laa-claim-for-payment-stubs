apiVersion: v1
kind: Service
metadata:
  name: {{ include "stubs-civil-claims-api.fullname" . }}
  labels:
    {{- include "stubs-civil-claims-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stubs-civil-claims-api.selectorLabels" . | nindent 4 }}
