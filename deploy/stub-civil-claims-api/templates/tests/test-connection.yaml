apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stubs-civil-claims-api.fullname" . }}-test-connection"
  labels:
    {{- include "stubs-civil-claims-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stubs-civil-claims-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
