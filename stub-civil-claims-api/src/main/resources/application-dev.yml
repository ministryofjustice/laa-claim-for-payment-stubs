logging.level.root: DEBUG
security:
  enabled: false   # feature-flag flip to true when the client is ready
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ISSUER_BASE_URL:"https://laa-claim-for-payment-stub-oidc-server-dev.cloud-platform.service.justice.gov.uk"}
          audience: ${AUDIENCE:api-audience}